# 릴리즈 노트

## v1.4.0 — 2026-09-20
### 추가
- **같은 도구로 다시 클릭하면 스티치 해제(토글)**: 풀/하프/쿼터/3·4 스티치 도구로 클릭(또는 드래그 시작)했을 때, 그 칸이 이미 지금 색·종류와 완전히 똑같으면 다시 칠하는 대신 지워짐. 실수로 옆 칸을 칠했을 때 지우개로 바꾸지 않고 같은 도구로 한 번 더 눌러서 바로 취소 가능. 다른 색이나 다른 스티치 종류로 누르면 평소처럼 덮어써짐. 드래그 중 지나가는 칸에는 영향 없음(스트로크 시작 칸 기준으로만 판단).
- **캔버스 크기 변경에 가로세로 비율 고정(사슬 아이콘)**: "캔버스 크기 변경" 모달에서 가로·세로 입력 사이의 링크 아이콘을 누르면 잠금, 한쪽 값을 바꾸면 비율대로 다른 쪽도 같이 바뀜.

## v1.3.0 — 2026-09-20
### 추가
- **툴바에서 바로 색 선택**: 도구막대 아래쪽에 현재 실 스와치 + 최근 쓴 색 2개 + "실 선택" 버튼 + 스포이드가 추가됨. "실 선택"을 누르면 이미 쓰는 실 목록(DMC 코드 포함)에서 바로 고르거나 "+ 새 실 추가"로 DMC 검색까지 한 번에 할 수 있음. 우측 패널까지 안 가도 됨.
### 수정
- **모바일 사파리(아이폰)에서 모달 하단 버튼이 화면 밖으로 잘리는 문제 수정.** 사파리 주소창이 떠 있을 때 뷰포트 높이 계산이 어긋나면서 "사진으로 만들기" 등 모달의 확인 버튼이 화면 밖으로 밀려나고 스크롤로도 닿지 않던 문제. 모달 바깥 영역이 스크롤 가능하도록 바꾸고, 지원 브라우저에서는 `100dvh`(동적 뷰포트 높이)를 사용해 실제 보이는 영역 기준으로 크기를 계산하도록 함.

## v1.2.0 — 2026-09-20
### 추가
- **자동 백업 폴더 (선택, 크롬/엣지)**: "내 도안" 화면에서 폴더를 한 번 지정해두면 도안이 바뀔 때마다 그 폴더에 백업 JSON을 자동으로 다시 씀. 구글드라이브 등 클라우드 동기화 폴더를 고르면 결과적으로 자동 클라우드 백업이 됨. 로그인 상태(로컬/클라우드) 어느 쪽이든 현재 라이브러리 전체를 백업함.
### 주의
- PNG/PDF/JSON 내보내기에 저장 위치 선택 다이얼로그(File System Access API)를 붙이려 했으나, 캔버스 인코딩·PDF 생성처럼 콜백이 여러 단계 중첩된 흐름에서 응답이 멈추는 현상이 확인되어 되돌림. 대신 크롬/엣지의 "다운로드 전 저장 위치 확인" 브라우저 설정을 켜서 같은 효과를 낼 수 있음(`docs/TODO_USER.md` 참고). 내보내기는 항상 기존 방식(다운로드 폴더)으로 동작해 안전함.
- 자동 백업 폴더 선택은 버튼 클릭에서 바로 호출되는 단순한 흐름이라 위 문제가 없음을 확인함.

## v1.1.0 — 2026-09-20
### 추가
- **여러 기기 동기화 (선택)**: 우측 상단 "Google로 로그인" 시 도안을 Firebase(Firestore)로 실시간 동기화. 오프라인에서도 계속 읽고 쓸 수 있고, 인터넷 연결되면 자동으로 반영됨.
- 최초 로그인 시 그 기기에만 있던 로컬 도안을 계정으로 업로드할지 확인하는 모달 추가.
- 로그인 상태에 맞춰 "내 도안" 화면의 저장 위치 안내 문구가 자동으로 바뀜(이 기기 vs Google 계정).
### 주의
- 로그인하지 않으면 이전과 완전히 동일하게 로컬/오프라인으로만 동작하며 어떤 데이터도 외부로 전송되지 않음.
- 이 기능은 저장소 소유자가 Firebase 프로젝트를 만들고 `firebase-config.json`을 채워야 실제로 켜짐. 설정 전까지는 로그인 버튼이 보이지 않음(기존과 동일). 설정 방법은 `docs/TODO_USER.md` 참고.

## v1.0.0 — 2026-09-20 (최초 릴리즈)
### 추가
- 사진 → 십자수 도안 변환 (가로 스티치 수, 최대 색 수, 디더링, 배경 제거, 외톨이 점 정리)
- DMC 454색 자동 매칭 (CIEDE2000)
- 편집 도구: 풀/하프/쿼터/3/4 스티치, 백스티치, 프렌치 노트, 지우개, 채우기, 스포이드, 선택·복사·붙여넣기·반전, 좌우/상하 대칭, 되돌리기/다시하기
- 보기: 컬러 / 기호 / 컬러+기호, 10칸 굵은 선, 눈금 번호, 중앙 표시, 확대·축소(핀치 지원)
- 실 목록: 기호, DMC 번호·이름, 스티치 수, 필요 타래 추정, 색 교체·합치기·기호 변경
- 원단: Aida 11~20ct, 린넨 28/32ct(2올) 완성 크기·재단 크기 계산
- 내 도안: 저장(자동), 열기, **복제 후 수정**, 이름 변경, 삭제, JSON 내보내기/불러오기, 전체 백업
- 진행 체크 모드 (수놓은 칸 표시, 진행률)
- 출력: PDF(표지·범례·분할 도안 페이지), PNG
- PWA: 설치형, 오프라인 동작, 새 버전 자동 감지·적용 배너
